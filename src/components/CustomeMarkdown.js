import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const CustomeMarkdown = () => {
  return (
    <View>
      <Text>CustomeMarkdown</Text>
    </View>
  );
};

export default CustomeMarkdown;

const styles = StyleSheet.create({});
